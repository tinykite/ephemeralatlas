import blockContent from './blockContent'
import category from './category'
import post from './post'
import tag from './tag'
import page from './page'

export const schemaTypes = [post, page, category, blockContent, tag]
